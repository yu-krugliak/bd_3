@model IEnumerable<LAB3.Models.Animals>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AN_NAME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AN_HEIGHT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AN_WEIGHT)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AN_NAME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AN_HEIGHT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AN_WEIGHT)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AN_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AN_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AN_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
