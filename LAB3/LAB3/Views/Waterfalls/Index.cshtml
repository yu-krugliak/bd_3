@model IEnumerable<LAB3.Models.Waterfalls>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WF_NAME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WF_HEIGHT)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WF_NAME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WF_HEIGHT)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WF_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WF_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WF_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
